# MongoDB/DocumentDB Connection
MONGODB_URI=mongodb://localhost:27017
DB_NAME=anybase

# JWT Configuration
JWT_SECRET=change-this-secret-in-production
JWT_EXPIRY=24h
REFRESH_TOKEN_EXPIRY=168h

# Initial Admin Setup (only used on first run)
INIT_ADMIN_EMAIL=admin@anybase.local
INIT_ADMIN_PASSWORD=changeme123

# Server Configuration
PORT=8080
HOST=0.0.0.0
MODE=development

# Dashboard Configuration (for Next.js)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional: AWS DocumentDB
# MONGODB_URI=mongodb://username:password@docdb-cluster.region.docdb.amazonaws.com:27017/?tls=true&tlsCAFile=rds-ca-2019-root.pem&retryWrites=false